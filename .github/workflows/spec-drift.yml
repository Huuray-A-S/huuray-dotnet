name: Spec drift

# Re-downloads the live v4 specification and opens a pull request if it changed.
#
# This is the early-warning system for API changes: because the conformance gates are
# checked against the vendored spec, a diff here is the first sign that a field, an
# endpoint, or a schema moved. Review every one of these PRs — do not merge on green alone.

on:
  schedule:
    - cron: '0 6 * * 1' # Mondays, 06:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4
        with:
          dotnet-version: |
            8.0.x
            9.0.x

      - name: Download the live spec
        run: bash ./scripts/fetch-spec.sh

      - name: Detect a change
        id: diff
        run: |
          if git diff --quiet -- openapi/huuray-v4.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "Spec unchanged."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            git diff --stat -- openapi/huuray-v4.json
          fi

      - name: Verify the SDK against the new spec
        if: steps.diff.outputs.changed == 'true'
        id: verify
        continue-on-error: true
        run: dotnet test Huuray.sln --configuration Release

      - name: Open a pull request
        if: steps.diff.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@22a9089034f40e5a961c8808d113e2c98fb63676 # v7
        with:
          # SPEC_DRIFT_TOKEN should be a fine-grained PAT (contents + pull requests:
          # write). Two reasons the default token is not enough: org repos block
          # Actions-created PRs unless a setting is flipped, and PRs opened with
          # GITHUB_TOKEN trigger no ci.yml run at all — the drift PR would show zero
          # checks. Falls back to GITHUB_TOKEN so the workflow still functions before
          # the secret exists.
          token: ${{ secrets.SPEC_DRIFT_TOKEN || github.token }}
          branch: spec-drift
          title: 'The Huuray v4 specification changed'
          commit-message: 'chore: sync openapi/huuray-v4.json with the live spec'
          labels: spec-drift
          body: |
            The live v4 specification differs from the vendored copy.

            **Suite against the new spec: ${{ steps.verify.outcome }}**

            - `success` — the change is backwards compatible with the current client.
              Still read the diff: a new endpoint will fail the coverage gate, and a
              new field may be worth exposing.
            - `failure` — the change breaks something. The conformance gates are doing
              their job; work out what moved before merging.

            Checklist:

            - [ ] Read the specification diff
            - [ ] New endpoints have SDK methods, or the coverage gate is failing for a reason we accept
            - [ ] Removed or renamed fields are handled, and the change is noted in `CHANGELOG.md`
            - [ ] `Spec.Validate` still understands every schema shape in the new document
            - [ ] `info.version` is still `v4` — this client targets v4 only
